name: figma_make_file
description: A new Flutter project.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Firebase Core and Services
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0
  firebase_auth: ^5.0.0
  
  # UI & Icons (Replacing Radix/Lucide-React)
  lucide_icons: ^1.0.0
  google_fonts: ^6.0.0
  
  # State & Forms (Replacing React-Hook-Form/Hooks)
  flutter_hooks: ^0.21.0
  riverpod: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
